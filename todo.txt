### TODO:
	* 安装时，把makefile完善一下.
		* pattern 匹配问题: 对pb目录的动态匹配.(输出目录统一为out，而不是单独成目录,但现在的匹配方式做不到)
		* 完成后，增加-O2的优化编译选项.(性能测试时添加，需要先经过功能测和压测)
		* 完成根目录的makefile.
	* 基本使用看example，但example后续还需要对应的完善，所以暂时就提示一下看example即可.
		* 整理example 也可以列出一些任务
	* client消息发到route时，如果server还没有注册，现在是只有route端有一个警告，后续需要在告知client一个通知
		* 具体设计还没出来.
	* 整理日志，使日志能够帮助运行时检查报错.
	* 异常情况的处理
		* 确认报错方式：日志，异常，消息?
		* 测试已有的每个异常
	* 压力测试，不看性能，主要是看一下压力情况是否会出现bug
		* gprof(GNU 性能测试工具), valgrind

### 其他项目对此项目的影响 or 需要完成的备忘.
	* 命名统一化问题：
		* 检查命名冲突，确定命名，修改.
		* 已知:
			* route 和 gate的含义相同。最终认为应该作为route定义，但老代码部分还是gate
			* package 和 packet
	* frnet 的stop问题.
		* 同线程递归调用到join。具体见frnet，会直接影响rpc OnReceive 中不能进行Stop操作.
	* frvim 
		* cs link 还存在bug。有时候会连接很多个.
		* 自动读取功能好像有时候不会生效.
	* 同步channel机制未实现.
	* log 变成json格式.

### 现有问题和进度 
	* pattern匹配问题. 对common 和 pb两个目录的匹配.

