### TODO:
	* 完善所有的测试用例.
	* 增加每一个版本的demo(在测试用例完成后)

### 不足需要补充(不做不会影响使用，属于设计缺陷，或代码整洁问题.).
	* network 更换后, 调整一下头文件. 部分头文件应该可以移动到cpp中.(现在暴露主要是hpsocket存在一些头文件顺序的依赖.)
	* 同步channel机制未实现.
	* 文档和流程 : use graph-easy

### 其他项目对此项目的影响 or 需要完成的备忘.
	* makefile pattern 匹配问题.
	* frnet 的stop问题.
		* 同线程递归调用到join。具体见frnet，会直接影响rpc OnReceive 中不能进行Stop操作.
	* frvim 
		* cs link 还存在bug。有时候会连接很多个.
		* cpp 生成时 是中文。这个修改成英文.
		* 自动读取功能好像有时候不会生效.

### 需要测试
	* 心跳部分：
		* 心跳尝试独立成一个类：发送函数可以作为一个回调传入.
		* 分为主发和检测：超时断线也作为函数传入.
	* route server
		* 注册
		* 断线需要消息通知
	* channel 
		* 断线等network响应对应都未完善.

