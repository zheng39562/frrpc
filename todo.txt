### TODO:
	* 需要测试
		* rpc请求服务
		* 连接，断开，以及相关的通知功能.
			* 两种模式的通知机制应该存在不同的情况.
		* 考虑大量传输的可能性.
		* 心跳部分:
			* 心跳尝试独立成一个类：发送函数可以作为一个回调传入.
			* 分为主发和检测：超时断线也作为函数传入.
		* channel 
			* 断线等network响应对应都未完善.
	* client消息发到route时，如果server还没有注册，现在是只有route端有一个警告，后续需要在告知client一个通知
		* 具体设计还没出来.
	* 压测和边界测试.
	* 文档和流程 : use graph-easy
	* method_index change to method name.
	* 同步channel机制未实现.

### 其他项目对此项目的影响 or 需要完成的备忘.
	* makefile 
		* pattern 匹配问题: 对pb目录的动态匹配.(输出目录统一为out，而不是单独成目录,但现在的匹配方式做不到)
		* 完成后，增加-O2的优化编译选项.(性能测试时添加，需要先经过功能测和压测)
	* frnet 的stop问题.
		* 同线程递归调用到join。具体见frnet，会直接影响rpc OnReceive 中不能进行Stop操作.
	* frvim 
		* cs link 还存在bug。有时候会连接很多个.
		* 自动读取功能好像有时候不会生效.

### 现有问题和进度 
	* client - route - server 之间的联通已完成。
		-= 还没测试之前发送数据包.
	* 当前停留状态： 
		* 基本消息调通了.

