### TODO:
	* network 更换后，调整一下头文件。部分头文件应该可以移动到cpp中。(现在暴露主要是hpsocket存在一些头文件顺序的依赖。)
	* 实现gate.
	* Channel 
		* 同步channel机制未实现.
		* 注册机制需要再思考一下怎么做 : 可能需要对应stub去支持, 应该个AddServer差不多才可以做到.
			* 当前这部分代码被注释了.先完成主体再说.
	* frnet 的stop问题.
		* 同线程递归调用到join。具体见frnet，会直接影响rpc OnReceive 中不能进行Stop操作.
	* 完善所有的测试用例.
	* 增加每一个版本的demo(在测试用例完成后)

	* frvim 
		* cs link 还存在bug。有时候会连接很多个。
		* cpp 生成时 是中文。这个修改成英文.
		* 自动读取功能好像有时候不会生效.

### 不足需要补充(不做不会影响使用，但设计上存在缺陷.).
	* Controlller : 功能定位还不算特别明确.

