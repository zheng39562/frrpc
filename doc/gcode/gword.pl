#! /usr/bin/perl

package gword;

#########################################################################
#
#
# is englist 是否使用英文.
# 后续会考虑增加英文版本.
sub isE(){ return 0; }

sub route(){ return "路由"; }
sub client(){ return "客户端"; }
sub server(){ return "服务器"; }

__END__

